{
  "framework": "mocha",
  "src_files": [
    "*.js",
    "test/*.spec.js"
  ],
  "launchers": {
    "Mocha": {
      "command": "node_modules/.bin/istanbul cover node_modules/mocha/bin/_mocha --growl --print none --report html test/*.spec.js -R tap"
    }
  },
  "launch_in_dev": [
    "Mocha"
  ]
}
